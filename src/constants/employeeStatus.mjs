export const EMPLOYEE_STATUS = Object.freeze({
    ACTIVE: 'active',
    INACTIVE: 'inactive',
    ON_LEAVE: 'on_leave',
    LEFT: 'left',
});

export const EMPLOYEE_INVITATION_STATUS = Object.freeze({
    PENDING: 'pending',
    SENT: 'sent',
    ACCEPTED: 'accepted',
    EXPIRED: 'expired',
    REVOKED: 'revoked',
});
